diff --git a/node_modules/aternos-unofficial-api/dist/index.js b/node_modules/aternos-unofficial-api/dist/index.js
index 56ceb39..968292e 100644
--- a/node_modules/aternos-unofficial-api/dist/index.js
+++ b/node_modules/aternos-unofficial-api/dist/index.js
@@ -48,11 +48,21 @@ function resolveChromiumPath() {
     "../../stable-chromium/chrome-linux/chrome-linux64/chrome"
   );
   if (import_fs.default.existsSync(localPath)) return localPath;
+  if (process.env.CHROME_PATH && import_fs.default.existsSync(process.env.CHROME_PATH)) return process.env.CHROME_PATH;
+  if (process.env.PUPPETEER_EXECUTABLE_PATH && import_fs.default.existsSync(process.env.PUPPETEER_EXECUTABLE_PATH)) return process.env.PUPPETEER_EXECUTABLE_PATH;
+  const candidates = [
+    "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe",
+    "C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe",
+    "C:\\Program Files\\Microsoft\\Edge\\Application\\msedge.exe",
+    "C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe",
+    process.env.LOCALAPPDATA ? import_path.default.resolve(process.env.LOCALAPPDATA, "Google/Chrome/Application/chrome.exe") : null
+  ].filter(Boolean);
+  for (const c of candidates) if (import_fs.default.existsSync(c)) return c;
   try {
-    return (0, import_child_process.execSync)("which chromium").toString().trim();
+    const cmd = process.platform === "win32" ? "where chromium" : "which chromium";
+    return (0, import_child_process.execSync)(cmd).toString().trim();
   } catch {
-    console.error("Chromium not found. Install with apt.");
-    process.exit(1);
+    throw new Error("Chromium not found. Install Chrome or Edge, or set CHROME_PATH.");
   }
 }
 function getFreePort() {
@@ -271,7 +281,7 @@ async function manageServer(cookies, serverId, task) {
             const timeRemaining = await page.evaluate(() => document.querySelector(".server-end-countdown")?.textContent?.trim() || "N/A");
             return { success: true, message: "Server is already online.", status: "Online", timeRemaining };
           }
-          if (currentStatus !== "Offline") {
+          if (currentStatus !== "Offline" && currentStatus !== null && currentStatus !== "Loading") {
             return { success: false, message: `Server is not offline. Current status: ${currentStatus}` };
           }
         }
